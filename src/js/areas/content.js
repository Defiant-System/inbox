
// mail.content

{
	init() {
		this.els = {
			el: window.find("content .wrapper"),
		};
	},
	dispatch(event) {
		let APP = mail,
			Self = APP.content,
			el;
		switch (event.type) {
			case "render-render-folder":
				break;
		}
	}
}
