
// mail.sidebar

{
	init() {
		this.els = {
			el: window.find("sidebar .wrapper"),
		};
	},
	dispatch(event) {
		let APP = mail,
			Self = APP.sidebar,
			el;
		switch (event.type) {
			case "render-render-folder":
				break;
		}
	}
}
