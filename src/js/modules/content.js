
// mail.content

{
	init() {
		this.els = {
			el: window.find("content"),
		};
	},
	async dispatch(event) {
		let APP = mail,
			Self = APP.content,
			el;
		switch (event.type) {
			case "event-type":
				break;
		}
	}
}
